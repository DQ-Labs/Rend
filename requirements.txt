customtkinter==5.2.2
# CTkImage needs Pillow to load the PNGs in assets/. CustomTkinter does NOT
# declare it as a dependency (it only requires darkdetect + packaging), so
# without this line the build succeeds and the app fails at runtime.
pillow==12.1.0
soundfile==0.13.1
torch==2.9.1
# RoFormer engine — in-process inference with the vendored architecture in
# roformer_source/ (see roformer_source/mel_band_roformer.py for lineage).
# All light and additive; no librosa/numba (mel filterbank is reimplemented).
einops==0.8.1
rotary-embedding-torch==0.9.1
beartype==0.22.9
# demucs and its dependencies are installed from source via setup_dev.ps1
# (see README "From Source" — install these first, then run .\setup_dev.ps1)
